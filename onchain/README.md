# onchain (Foundry)

Contains verifier contract and tests.

- `contracts/Verifier.sol` is generated by `zk/cmd/gnark-basic`.
- `test/GnarkFixture.sol` is generated with proof/public input constants.

Run tests:

```bash
cd /Users/user0/Code/tk/gnark-playground/onchain
forge test
```
